"""
_archive — INERT. No code in this directory may be imported or executed.

All files here are quarantined legacy code from ISI v0.1 and earlier.
They are retained for historical reference ONLY.

If you are importing from _archive, you have a bug.
"""

raise ImportError(
    "FATAL: Attempted to import from _archive/. "
    "All code in _archive/ is quarantined legacy and MUST NOT be imported. "
    "If you see this error, your code has an import that references "
    "deprecated/archived modules. Fix the import to use the canonical "
    "backend/ or pipeline/ packages instead."
)
